@model Meshkah.ViewModels.PointsTransactionVM;

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PointsTransaction</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PointId" class="control-label"></label>
                <select asp-for="PointId" class ="form-control" asp-items="ViewBag.PointId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Selected_Users" class="control-label"></label>
                <select asp-for="Selected_Users" class ="form-control" asp-items="ViewBag.UserId" multiple>
                    <option selected disabled>--Select--</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
